<?xml version="1.0" encoding="UTF-8" ?>
<configuration>
  <app>
    <title>${project.name}</title>
    <version>${project.version}</version>
    <build>${maven.build.timestamp}</build>
  </app>
  <templates>
    <show-at-startup>true</show-at-startup>
  </templates>
</configuration>
